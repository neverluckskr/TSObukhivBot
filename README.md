# TSObukhivBot

Telegram-бот «Тёмная сторона Обухова» для модерации и публикации постов в Telegram-канал с поддержкой платных публикаций через Telegram Stars и Smart Glocal.

## 🚀 Возможности

- ✅ Бесплатные анонимные посты
- 💰 Платные рекламные посты (35 грн/Stars)
- 🎯 Платные офтопик посты (50 грн/Stars)
- 🔍 Модерация постов
- 💳 Интеграция с Telegram Stars
- 📊 Статистика для модераторов
- 🚫 Система банов пользователей

## 📋 Требования

- Python 3.10+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- ID канала для публикации постов
- ID модераторов

## 🛠️ Установка

1. **Клонируйте репозиторий или скачайте файлы**

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

3. **Создайте файл `.env` на основе `.env.example`:**
```bash
cp .env.example .env
```

4. **Заполните `.env` файл:**
```env
BOT_TOKEN=your_bot_token_here
CHANNEL_ID=your_channel_id_here
MODERATORS=123456789,987654321
```

5. **Запустите бота:**
```bash
python bot.py
```

## 📁 Структура проекта

```
telegram_bot/
├── bot.py                 # Точка входа
├── config.py              # Конфигурация
├── database/
│   ├── models.py          # ORM модели
│   └── db.py              # Подключение к БД
├── handlers/
│   ├── user.py            # Обработчики пользователей
│   ├── moderator.py       # Обработчики модераторов
│   └── payments.py        # Обработка платежей
├── keyboards/
│   ├── user_kb.py         # Клавиатуры для пользователей
│   └── moderator_kb.py    # Клавиатуры для модераторов
├── states/
│   └── states.py          # FSM состояния
├── utils/
│   ├── texts.py           # Текстовые константы
│   └── helpers.py         # Вспомогательные функции
└── requirements.txt
```

## 📝 Команды бота

### Для пользователей:
- `/start` — Запуск бота, главное меню
- `/send` — Отправить бесплатный анонимный пост
- `/send35` — Отправить рекламный пост (35 грн/Stars)
- `/send50` — Отправить пост не по тематике (50 грн/Stars)
- `/help` — Подробности о боте
- `/cancel` — Отменить текущее действие

### Для модераторов:
- `/stats` — Статистика постов
- `/moderator` — Панель модерации (просмотр постов по-странично, навигация, одобрение/отклонение/редактирование)

> Новые возможности панели модератора:
> - Пагинация и навигация между постами (◀️/▶️)
> - Просмотр информации о пользователе и его постов
> - Подтверждение опасных действий (бан, массовое одобрение)
> - Быстрые действия: удалить пост, забанить/разбанить

## 🔧 Настройка

### Получение ID канала:
1. Добавьте бота в канал как администратора
2. Отправьте любое сообщение в канал
3. Перешлите это сообщение боту [@userinfobot](https://t.me/userinfobot)
4. Скопируйте ID канала (формат: `-1001234567890`)

### Получение ID модераторов:
1. Попросите модератора написать боту [@userinfobot](https://t.me/userinfobot)
2. Скопируйте его user_id
3. Добавьте в `.env` через запятую

## 💳 Настройка платежей

### Telegram Stars:
Работает из коробки, дополнительная настройка не требуется.

### Stripe (опционально):
1. Зарегистрируйтесь на [Stripe](https://stripe.com)
2. Получите API ключи
3. Добавьте в `.env`:
```env
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLIC_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
```

**Примечание:** Интеграция со Stripe требует дополнительной настройки webhook и обработчика платежей.

## 🗄️ База данных

По умолчанию используется SQLite (`bot.db`). Для продакшена рекомендуется PostgreSQL:

```env
DATABASE_URL=postgresql+asyncpg://user:password@localhost/dbname
```

Не забудьте установить `asyncpg`:
```bash
pip install asyncpg
```

## 📊 База данных

Бот автоматически создает следующие таблицы:
- `users` — пользователи
- `posts` — посты
- `payments` — платежи
- `moderators` — модераторы

## 🚀 Деплой на Railway

Бот готов к деплою на Railway. Подробные инструкции в файле [DEPLOY.md](DEPLOY.md).

### Быстрый деплой:

1. Создайте проект на [railway.app](https://railway.app)
2. Подключите GitHub репозиторий
3. Добавьте переменные окружения:
   - `BOT_TOKEN`
   - `CHANNEL_ID`
   - `MODERATORS`
   - `PROVIDER_TOKEN` (опционально)
4. Railway автоматически соберет и запустит бота

## 🐳 Docker

Dockerfile уже включен в проект. Для локального запуска:

```bash
docker build -t tso-bot .
docker run -e BOT_TOKEN=your_token -e CHANNEL_ID=your_channel_id tso-bot
```

## 📝 Лицензия

MIT License

## 🤝 Поддержка

При возникновении проблем создайте issue в репозитории или обратитесь к администратору.

